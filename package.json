{
  "name": "degree-planner",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "npm-run-all --parallel dev:client dev:server",
    "dev:client": "cd services/client && yarn dev",
    "dev:server": "cd services/server && yarn dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
